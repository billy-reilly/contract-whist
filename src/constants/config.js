import { Map } from 'immutable';

const config = new Map({
    mainContentColWidths: {
        sm: 8,
        smOffset: 2,
        md: 6,
        mdOffset: 3,
        lg: 4,
        lgOffset: 4
    }
});

export default config;
